// filter anagrams

function filterAnagrams(arr, target) {

    // count letters in each word of the words array
  
}

const words = ['listen', 'silent', 'dog', 'god', 'hello', 'world'];
const target = 'enlist';

const anagrams = filterAnagrams(words, target);
